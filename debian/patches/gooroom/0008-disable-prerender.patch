From fda8ae8daded6063ae0c6fcf8da6855733a2131e Mon Sep 17 00:00:00 2001
From: junsungc <junsungc@gooroom.kr>
Date: Wed, 22 Jul 2020 15:33:29 +0900
Subject: [PATCH 08/84] disable prerender

---
 chrome/browser/ui/omnibox/chrome_omnibox_client.cc | 2 ++
 1 file changed, 2 insertions(+)

diff --git a/chrome/browser/ui/omnibox/chrome_omnibox_client.cc b/chrome/browser/ui/omnibox/chrome_omnibox_client.cc
index bc9336a74bbf..90f530b5f8c5 100644
--- a/chrome/browser/ui/omnibox/chrome_omnibox_client.cc
+++ b/chrome/browser/ui/omnibox/chrome_omnibox_client.cc
@@ -351,10 +351,12 @@ void ChromeOmniboxClient::OnTextChanged(const AutocompleteMatch& current_match,
       // (http://crbug.com/105689)
       if (!CurrentPageExists())
         break;
+#if !BUILDFLAG(ENABLE_GOOROOM)
       // Ask for prerendering if the destination URL is different than the
       // current URL.
       if (current_match.destination_url != GetURL())
         DoPrerender(current_match);
+#endif
       break;
     case AutocompleteActionPredictor::ACTION_PRECONNECT:
       DoPreconnect(current_match);
-- 
2.20.1

