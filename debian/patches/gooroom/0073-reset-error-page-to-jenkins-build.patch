From 8c9a3032170a49782924fb293f846fb8a11515fd Mon Sep 17 00:00:00 2001
From: junsungc <junsungc91@gmail.com>
Date: Fri, 23 Apr 2021 10:17:40 +0900
Subject: [PATCH 73/84] reset error page to jenkins build

---
 components/error_page/common/localized_error.cc | 15 +++++++--------
 1 file changed, 7 insertions(+), 8 deletions(-)

diff --git a/components/error_page/common/localized_error.cc b/components/error_page/common/localized_error.cc
index 1fa1e4651..2caa18d88 100644
--- a/components/error_page/common/localized_error.cc
+++ b/components/error_page/common/localized_error.cc
@@ -9,7 +9,6 @@
 #include <memory>
 #include <utility>
 
-#include "gooroom/URLRewrite/gooroom_url_rewrite.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
 #include "base/i18n/rtl.h"
@@ -947,17 +946,17 @@ LocalizedError::PageState LocalizedError::GetPageState(
     options.suggestions &= ~SUGGEST_LEARNMORE;
   }
 
-#if defined(GOOROOM_BUILD)
+//#if defined(GOOROOM_BUILD)
 //Change chrome:// to gooroom:// on error pages
-  GURL changed_failed_url = GooroomURLRewrite::ChangeGooroomScheme(failed_url);
-  base::string16 failed_url_string(url_formatter::FormatUrl(
-      changed_failed_url, url_formatter::kFormatUrlOmitNothing,
-      net::UnescapeRule::NORMAL, nullptr, nullptr, nullptr));
-#else
+//  GURL changed_failed_url = GooroomURLRewrite::ChangeGooroomScheme(failed_url);
+//  base::string16 failed_url_string(url_formatter::FormatUrl(
+//      changed_failed_url, url_formatter::kFormatUrlOmitNothing,
+//      net::UnescapeRule::NORMAL, nullptr, nullptr, nullptr));
+//#else
   base::string16 failed_url_string(url_formatter::FormatUrl(
       failed_url, url_formatter::kFormatUrlOmitNothing,
       net::UnescapeRule::NORMAL, nullptr, nullptr, nullptr));
-#endif
+//#endif
   // URLs are always LTR.
   if (base::i18n::IsRTL())
     base::i18n::WrapStringWithLTRFormatting(&failed_url_string);
-- 
2.20.1

