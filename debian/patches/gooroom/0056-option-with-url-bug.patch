From 94e05d7a49619bf0c3e8cc1ca0076f274900d0ae Mon Sep 17 00:00:00 2001
From: junsungc <junsungc@gooroom.kr>
Date: Mon, 27 Jul 2020 14:26:05 +0900
Subject: [PATCH 56/84] option with url bug

---
 gooroom/browser/gooroom_pref.cc | 26 --------------------------
 1 file changed, 26 deletions(-)

diff --git a/gooroom/browser/gooroom_pref.cc b/gooroom/browser/gooroom_pref.cc
index 75a92cd48..22a9222ce 100644
--- a/gooroom/browser/gooroom_pref.cc
+++ b/gooroom/browser/gooroom_pref.cc
@@ -107,32 +107,6 @@ void GooroomPref::setup() {
 
       break;
     }
-    if (GURL(*it).is_valid() == true) {
-      bool trusted = pfilterImpl->isTrustUrl(*it);
-      base::GetGooroomBase()->setTrustMode(trusted);
-      int pid;
-      if (trusted) {
-        pid = fork();
-        if (pid != 0) {
-          exit(0);
-        } else {
-          execl(exe_path.c_str(), "gooroom-browser",
-                trust_user_data_dir_option.c_str(), flash_dir.c_str(), GURL(*it).spec().c_str(),
-                NULL);
-          exit(0);
-        }
-      } else {
-        pid = fork();
-        if (pid != 0) {
-          exit(0);
-        } else {
-          execl(exe_path.c_str(), "gooroom-browser",
-                untrust_user_data_dir_option.c_str(),flash_dir.c_str(), GURL(*it).spec().c_str(),
-                NULL);
-          exit(0);
-        }
-      }
-    }
   }
   if (!use_user_data_dir) {
     const char** restartoption = new const char*[argv.size() + 2];
-- 
2.20.1

