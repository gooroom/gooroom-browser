From c61d8360f1b62577cfa3c36c273465c9f63020be Mon Sep 17 00:00:00 2001
From: junsungc <junsungc@gooroom.kr>
Date: Thu, 23 Jul 2020 17:23:10 +0900
Subject: [PATCH 24/66] disable chrome login shortcut(ctrl+shift+m) and hide
 login icon

---
 chrome/browser/ui/browser_command_controller.cc | 3 +++
 chrome/browser/ui/views/toolbar/toolbar_view.cc | 4 ++++
 2 files changed, 7 insertions(+)

diff --git a/chrome/browser/ui/browser_command_controller.cc b/chrome/browser/ui/browser_command_controller.cc
index dffc146633fd5..589235cc9502d 100644
--- a/chrome/browser/ui/browser_command_controller.cc
+++ b/chrome/browser/ui/browser_command_controller.cc
@@ -859,8 +859,11 @@ bool BrowserCommandController::ExecuteCommandWithDisposition(
       break;
 #if BUILDFLAG(ENABLE_DICE_SUPPORT)
     case IDC_SHOW_SIGNIN:
+#if !defined(GOOROOM_BUILD)
+//Disable chrome login shortcut (ctrl + shift + m) on gooroom-browser
       ShowBrowserSigninOrSettings(
           browser_, signin_metrics::AccessPoint::ACCESS_POINT_MENU);
+#endif
       break;
 #endif
     case IDC_DISTILL_PAGE:
diff --git a/chrome/browser/ui/views/toolbar/toolbar_view.cc b/chrome/browser/ui/views/toolbar/toolbar_view.cc
index ef34f041219b5..b5c37cef74e02 100644
--- a/chrome/browser/ui/views/toolbar/toolbar_view.cc
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.cc
@@ -289,6 +289,10 @@ void ToolbarView::Init() {
                                browser_->profile()->IsGuestSession();
 #elif BUILDFLAG(IS_CHROMEOS_LACROS)
   show_avatar_toolbar_button = !profiles::IsPublicSession();
+#endif
+#if defined(GOOROOM_BUILD)
+//Hide show_avatar_toolbar_button
+  show_avatar_toolbar_button = false;
 #endif
   if (base::FeatureList::IsEnabled(
           autofill::features::kAutofillEnableToolbarStatusChip)) {
-- 
2.30.2

