From eeadafa17c73f94f745dfe9b6da463fc02be2f68 Mon Sep 17 00:00:00 2001
From: junsungc <junsungc@gooroom.kr>
Date: Mon, 27 Jul 2020 16:10:50 +0900
Subject: [PATCH 80/88] [BTS:2322] Hide voice search

---
 chrome/browser/resources/local_ntp/local_ntp.js | 14 +++++++-------
 1 file changed, 7 insertions(+), 7 deletions(-)

diff --git a/chrome/browser/resources/local_ntp/local_ntp.js b/chrome/browser/resources/local_ntp/local_ntp.js
index 4c2e0fb67acc..2ed1105ba185 100644
--- a/chrome/browser/resources/local_ntp/local_ntp.js
+++ b/chrome/browser/resources/local_ntp/local_ntp.js
@@ -940,9 +940,10 @@ function init() {
           autocompleteMatchImageAvailable;
 
       if (!iframesAndVoiceSearchDisabledForTesting) {
-        speech.init(
-            configData.googleBaseUrl, configData.translatedStrings,
-            $(IDS.REALBOX_MICROPHONE), searchboxApiHandle);
+// Voice search is not supported on gooroom-browser
+//        speech.init(
+//            configData.googleBaseUrl, configData.translatedStrings,
+//            $(IDS.REALBOX_MICROPHONE), searchboxApiHandle);
       }
 
       utils.disableOutlineOnMouseClick($(IDS.REALBOX_MICROPHONE));
@@ -959,10 +960,9 @@ function init() {
           configData.translatedStrings.searchboxPlaceholder;
 
       if (!iframesAndVoiceSearchDisabledForTesting) {
-// Voice search is not supported on gooroom-browser
-//        speech.init(
-//            configData.googleBaseUrl, configData.translatedStrings,
-//            $(IDS.FAKEBOX_MICROPHONE), searchboxApiHandle);
+        speech.init(
+            configData.googleBaseUrl, configData.translatedStrings,
+            $(IDS.FAKEBOX_MICROPHONE), searchboxApiHandle);
       }
 
       // Listener for updating the key capture state.
-- 
2.20.1

