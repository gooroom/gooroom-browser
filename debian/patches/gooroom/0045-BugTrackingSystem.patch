From 1e2ef075448bdf5912ad74b23bc4e5d7f0f67d64 Mon Sep 17 00:00:00 2001
From: junsungc <junsungc@gooroom.kr>
Date: Mon, 27 Jul 2020 14:12:50 +0900
Subject: [PATCH 45/63] BugTrackingSystem

507 : Disable 'Open as window' on making shortcut app popup
---
 .../ui/views/web_apps/web_app_confirmation_view.cc     | 10 ++++++++--
 1 file changed, 8 insertions(+), 2 deletions(-)

diff --git a/chrome/browser/ui/views/web_apps/web_app_confirmation_view.cc b/chrome/browser/ui/views/web_apps/web_app_confirmation_view.cc
index 5acc8ddac919c..452def0f041f4 100644
--- a/chrome/browser/ui/views/web_apps/web_app_confirmation_view.cc
+++ b/chrome/browser/ui/views/web_apps/web_app_confirmation_view.cc
@@ -158,7 +158,9 @@ WebAppConfirmationView::WebAppConfirmationView(
                 IDS_BOOKMARK_APP_BUBBLE_OPEN_AS_TABBED_WINDOW))
             .SetGroup(kRadioGroupId)
             .SetChecked(display_mode == web_app::DisplayMode::kTabbed));
-  } else {
+  }
+#if !defined(GOOROOM_BUILD)
+  else {
     builder.AddChildren(
         views::Builder<views::View>(),  // Column skip.
         views::Builder<views::Checkbox>()
@@ -176,6 +178,7 @@ WebAppConfirmationView::WebAppConfirmationView(
     else
       open_as_window_checkbox_->SetChecked(true);
   }
+#endif
 
   title_tf_->SelectAll(true);
   chrome::RecordDialogCreation(chrome::DialogIdentifier::WEB_APP_CONFIRMATION);
@@ -207,11 +210,14 @@ bool WebAppConfirmationView::Accept() {
                                              ? web_app::DisplayMode::kStandalone
                                              : web_app::DisplayMode::kBrowser;
     }
-  } else {
+  }
+#if !defined(GOOROOM_BUILD)
+  else {
     web_app_info_->user_display_mode = open_as_window_checkbox_->GetChecked()
                                            ? web_app::DisplayMode::kStandalone
                                            : web_app::DisplayMode::kBrowser;
   }
+#endif
   std::move(callback_).Run(true, std::move(web_app_info_));
   return true;
 }
-- 
2.30.2

